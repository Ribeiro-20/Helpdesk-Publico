{
  "name": "base-monitor",
  "version": "1.0.0",
  "description": "Sistema de monitorização de anúncios de contratação pública do portal BASE",
  "private": true,
  "scripts": {
    "start": "npm run cron",
    "once": "npm run cron:once",
    "setup:base": "npx supabase start && npx supabase db reset && npm run import-cpvs",
    "setup": "npm run setup:base && copy /Y .env supabase\\functions\\.env && start \"BASE Monitor Functions\" cmd /k \"cd /d %CD% && npx supabase functions serve --env-file supabase/functions/.env\" && start \"BASE Monitor Web\" cmd /k \"cd /d %CD%\\apps\\web && npm install && npm run dev\"",
    "dev": "cd apps/web && npm install && npm run dev",
    "functions": "npx supabase functions serve --env-file supabase/functions/.env",
    "import-cpvs": "cd scripts && npm install && npx tsx import-cpvs.ts",
    "cron": "npm --prefix scripts install && npm --prefix supabase/cron install && npm --prefix supabase/cron run start",
    "cron:once": "npm --prefix scripts install && npm --prefix supabase/cron install && npm --prefix supabase/cron run once",
    "cron:hubspot:once": "npm --prefix scripts install && npm --prefix supabase/cron install && npm --prefix supabase/cron run hubspot:once",
    "market:refresh": "node scripts/market-refresh.js",
    "scrape-dr": "npm --prefix scripts run scrape-dr --",
    "scrape-dr:upsert": "npm --prefix scripts run scrape-dr:upsert --"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.44.2",
    "@types/node": "^20.11.0",
    "dotenv": "^16.4.5",
    "exceljs": "^4.4.0",
    "supabase": "^2.81.3",
    "tsx": "^4.19.1"
  }
}
